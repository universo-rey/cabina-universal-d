# Guia De Fronteras

Permitido ahora:

- crear carpetas locales;
- crear documentos iniciales;
- revisar y ajustar el mapa.
- validar repos y matrices locales;
- branch, commit, push, PR draft/update, issues, labels, comentarios y readbacks en GitHub cuando sea repo-visible, reversible, trazable y validado.

No permitido ahora sin orden separada:

- mover clones;
- ejecutar Microsoft live;
- escribir SharePoint, Power Platform, Graph, Teams, Outlook o tenant;
- llamar OpenAI API live;
- crear agentes remotos persistentes;
- cambiar permisos, produccion, merges protegidos, force push o delete branch remoto;
- guardar secretos.
