# Work Queue Classification Batch

## Estado
BATCH_PREPARED_NOT_SUBMITTED

## Boundary
This JSONL is metadata-only and sanitized. It uses source_path_hash, not raw source paths. It contains no secrets, tokens, documents, SharePoint items, Planner tasks, Graph dumps or regulated data.

## Submission
Not submitted. A separate cost/submit order is required before Batch API upload.
